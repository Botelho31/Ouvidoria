import styled from 'styled-components'
import { View } from 'react-native'

// Spacer, view que deixa simétrico a distancia entre duas views
const Spacer = styled(View)`
  flex: 1;
`

export default Spacer
